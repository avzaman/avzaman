# Optimizing the server
- I like dock-or poggies
- issues facing tho:
    - error on startup
        - job systemd-networkd-wait-onlin.service/start running
    - error on shutdown
        - job lvm2-monitor.service/stop -> failed command READ FROM QUEUE
    - can only shutdown and startup, cannot reboot!!!
